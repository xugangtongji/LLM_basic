# 5.1 SFT 指令微调

> **所属章节**：第五章 后训练与对齐 > 5.1 SFT 指令微调
> **预计阅读时间**：55-70 分钟
> **前置知识**：4.3 分布式训练、3.4 Multi-Head Attention（了解模型结构即可）
> **本章目标**：理解 SFT 和预训练的本质区别；掌握 Chat Template 和 Loss Masking 的实现；能用 LoRA/QLoRA 在消费级显卡上微调 7B 模型；知道全量微调和 LoRA 的适用场景

---

## 零、开篇：预训练模型 ≠ 可用的 AI 助手

预训练结束后，你得到了一个拥有巨大知识储备的"原始"语言模型。但这个模型：

- 让它"写一首诗"，它可能续写"……是古代文人的雅趣，诗歌的起源可以追溯到……"（纯续写模式）
- 让它"帮我检查这段代码"，它可能直接无视指令继续生成随机文字
- 让它"用中文回答"，它可能用英文回答（如果预训练数据以英文为主）

SFT（Supervised Fine-Tuning，监督微调）是把预训练模型变成指令跟随模型和对话模型的关键步骤。通过让模型在"指令→期望回答"的配对数据上继续训练，模型学会了"当用户说 X，我应该回应 Y"的行为模式。

LLM 的演进路线是：

```
预训练（Base Model）
    → SFT 指令微调（Instruct Model）   ← 本章
    → RLHF / DPO 对齐（Chat Model）    ← 后续章节
```

SFT 是连接"原始知识"和"可用助手"之间的桥梁。

---

## 一、SFT 的本质：教模型"说话的方式"

### 1.1 初印象：用费曼视角理解

**SFT 的目标不是教模型新知识，而是教它如何表达已有的知识**。

类比：一个博士生可能知道量子力学的所有内容，但不会用大白话解释给外行听。SFT 就是在教这个博士生"怎么和普通人对话"——不是教它新知识，而是改变它的表达方式。

SFT 把结构化的指令-回答对转换成统一的提示模板，将预训练的基础模型转变为指令跟随模型，最终成为具备自然对话能力的 Chat 模型。

预训练和 SFT 的根本区别：

| | 预训练 | SFT |
|--|--------|-----|
| 目标 | 学习语言的统计规律和世界知识 | 学习按指令格式回答 |
| 数据规模 | 万亿 token | 数万到数百万条指令对 |
| 训练时长 | 数周到数月 | 数小时到数天 |
| 学习率 | 较高（3e-4） | 较低（1e-5 到 1e-4）|
| Loss 计算 | 所有 token | **只对回答部分** |

### 1.2 Loss Masking：只从回答中学习

SFT 最关键的工程细节：**只对回答部分计算 Loss，忽略指令部分**。

为什么？指令（用户输入）是给定的条件，不是要预测的目标。如果对指令也计算 Loss，模型会学到"如何生成指令"而不是"如何回答指令"，效果会变差。

可以使用 DataCollatorForCompletionOnlyLM 类来排除指令部分的 Loss 计算。

```python
import torch
import torch.nn.functional as F
from transformers import AutoTokenizer

def create_sft_labels(
    input_ids: list[int],
    response_start_token_ids: list[int],
    ignore_index: int = -100,
) -> list[int]:
    """
    创建 SFT 训练标签：指令部分设为 ignore_index，只保留回答部分的 token id。
    
    input_ids: 完整的 token id 序列（指令 + 回答）
    response_start_token_ids: 标记回答开始的 token id 序列
    """
    labels = [ignore_index] * len(input_ids)   # 初始全部忽略
    
    # 找到回答开始的位置
    n = len(response_start_token_ids)
    for i in range(len(input_ids) - n + 1):
        if input_ids[i:i+n] == response_start_token_ids:
            # 从回答开始位置之后，把 label 设为实际的 token id
            for j in range(i + n, len(input_ids)):
                labels[j] = input_ids[j]
            break
    
    return labels

# 示例：Llama-3 风格的对话
# 指令部分：<|begin_of_text|><|start_header_id|>user<|end_header_id|>...<|eot_id|>
# 回答部分：<|start_header_id|>assistant<|end_header_id|>...<|eot_id|>
sample_input = "指令：写一首关于春天的诗。\n回答："
sample_response = "春风拂面柳絮飞，万紫千红映日晖。"

# 在实际代码里：
# input_ids = tokenizer(sample_input + sample_response)['input_ids']
# response_token_ids = tokenizer("回答：")['input_ids']
# labels = create_sft_labels(input_ids, response_token_ids)
# # labels 的前半段全是 -100，后半段才是真实 token id

print("Loss Masking 的效果：")
print("  指令部分的 label：-100（不计算 loss）")
print("  回答部分的 label：实际 token id（计算 loss）")
```

---

## 二、Chat Template：对话的格式规范

### 2.1 初印象：用费曼视角理解

**Chat Template 是"把多轮对话整理成模型能理解的单一文本序列"的格式规范**。

不同模型有不同的对话格式，用错格式就像在错误的语境里说话。用 LLaMA-3 的 prompt 格式去问 Qwen，模型会"听不懂"你的指令。

指令微调需要 Chat Template（定义如何把对话结构化为文本序列，包括角色标记、特殊 token 和轮次边界），以及包含指令-回答对的对话数据集。

### 2.2 主流模型的 Chat Template

```python
# 几种主流模型的 Chat Template 格式

# ── LLaMA-3 风格 ──
llama3_template = """<|begin_of_text|><|start_header_id|>system<|end_header_id|>

你是一个有帮助的 AI 助手。<|eot_id|><|start_header_id|>user<|end_header_id|>

请解释什么是反向传播？<|eot_id|><|start_header_id|>assistant<|end_header_id|>

反向传播是训练神经网络的核心算法...<|eot_id|>"""

# ── Qwen 风格 ──
qwen_template = """<|im_start|>system
你是 Qwen，由阿里巴巴云开发的 AI 助手。<|im_end|>
<|im_start|>user
请解释什么是反向传播？<|im_end|>
<|im_start|>assistant
反向传播是训练神经网络的核心算法...<|im_end|>"""

# ── ChatML 格式（通用，OpenAI 推广）──
chatml_template = """<|im_start|>system
你是一个有帮助的助手。<|im_end|>
<|im_start|>user
请解释什么是反向传播？<|im_end|>
<|im_start|>assistant
反向传播是训练神经网络的核心算法...<|im_end|>"""

print("不同模型的 Chat Template 格式各不相同，用错格式会导致模型不理解指令。")


# ── 用 HuggingFace Tokenizer 自动应用 Chat Template ──
from transformers import AutoTokenizer

def format_chat_with_template(messages: list[dict], 
                               model_name: str = "Qwen/Qwen2.5-7B-Instruct") -> str:
    """
    使用 HuggingFace tokenizer 的 apply_chat_template 自动格式化对话。
    messages 格式：[{"role": "system/user/assistant", "content": "..."}]
    """
    # tokenizer = AutoTokenizer.from_pretrained(model_name)
    # formatted = tokenizer.apply_chat_template(
    #     messages, 
    #     tokenize=False,           # 只返回字符串，不 tokenize
    #     add_generation_prompt=True  # 在结尾加上 "assistant:" 提示符
    # )
    # return formatted
    pass

# 示例对话数据
messages = [
    {"role": "system",    "content": "你是一个有帮助的 AI 助手，请用中文回答。"},
    {"role": "user",      "content": "什么是梯度下降？"},
    {"role": "assistant", "content": "梯度下降是一种优化算法..."},
    {"role": "user",      "content": "它和随机梯度下降有什么区别？"},
    # 最后没有 assistant 的回答 → 模型需要生成
]

print("\n对话数据格式示例：")
for msg in messages:
    print(f"  [{msg['role']}]: {msg['content'][:40]}...")
```

### 2.3 SFT 数据集格式

SFT 数据可以是单轮（一问一答）或多轮（多轮对话）：

```python
# 单轮指令数据（Alpaca 格式）
alpaca_example = {
    "instruction": "将以下句子翻译成英文：",
    "input": "深度学习正在改变人工智能领域。",
    "output": "Deep learning is transforming the field of artificial intelligence."
}

# 多轮对话数据（ShareGPT 格式）
sharegpt_example = {
    "conversations": [
        {"from": "human",  "value": "你好，能帮我解释一下什么是 Transformer 吗？"},
        {"from": "gpt",    "value": "当然可以！Transformer 是 2017 年由 Google 提出的..."},
        {"from": "human",  "value": "那 BERT 和 GPT 有什么区别？"},
        {"from": "gpt",    "value": "很好的问题！它们都基于 Transformer，但..."},
    ]
}

# 主流开源 SFT 数据集
sft_datasets = {
    "Alpaca（52K）":        "Stanford Alpaca，用 text-davinci-003 生成的指令数据",
    "FLAN（1000+ 任务）":   "Google，多任务指令数据，覆盖极广",
    "ShareGPT（90K）":      "真实 ChatGPT 对话，多轮，质量较高",
    "OpenHermes（1M+）":    "高质量综合指令数据集，常用于 Hermes 系模型",
    "UltraChat（200K）":    "长多轮对话，由 GPT-4 生成",
    "Magpie（1M+）":        "从对齐模型"空 prompt"中自动提取的高质量指令数据",
}

print("常用 SFT 数据集：")
for name, desc in sft_datasets.items():
    print(f"  {name}: {desc}")
```

---

## 三、LoRA：参数高效微调的核心方法

### 3.1 初印象：用费曼视角理解

**LoRA（Low-Rank Adaptation）是"不修改原始参数，而是旁边加一个小的旁路网络"**。

全量微调（Full Fine-tuning）：修改模型的所有参数（7B 参数全部更新）。代价：需要和预训练一样多的显存（梯度 + 优化器状态 ≈ 7B × 12 字节 ≈ 84 GB）。

LoRA 的想法：不直接修改 $W$，而是在旁边加两个小矩阵 $A$ 和 $B$，让真正的参数更新发生在这两个小矩阵里：

$$W' = W + \Delta W = W + BA$$

其中 $A \in \mathbb{R}^{d \times r}$，$B \in \mathbb{R}^{r \times d}$，$r$（秩/rank）远小于 $d$（通常 4-64）。

$W$ 的参数量是 $d^2$，而 $BA$ 的参数量只有 $2dr$。当 $r=16$，$d=4096$ 时：
- $W$：$4096^2 \approx 1680$ 万参数
- $BA$：$2 \times 4096 \times 16 = 13$ 万参数
- **减少约 128 倍**

```python
import torch
import torch.nn as nn
import math

class LoRALinear(nn.Module):
    """
    带 LoRA 适配器的线性层。
    原始权重 W 冻结，只训练 A 和 B 两个小矩阵。
    """
    def __init__(
        self,
        in_features: int,
        out_features: int,
        rank: int = 16,          # LoRA 秩（越小参数越少，越大容量越强）
        alpha: float = 32.0,     # 缩放系数（通常设为 rank 的 2 倍）
        dropout: float = 0.05,
    ):
        super().__init__()
        self.rank  = rank
        self.scale = alpha / rank   # 实际更新的缩放因子

        # 原始权重（冻结，不参与梯度计算）
        self.weight = nn.Parameter(
            torch.randn(out_features, in_features) * 0.02,
            requires_grad=False   # 关键：冻结！
        )

        # LoRA 小矩阵（可训练）
        self.lora_A = nn.Parameter(torch.randn(in_features, rank) / math.sqrt(rank))
        self.lora_B = nn.Parameter(torch.zeros(rank, out_features))  # B 初始化为 0
        # B 初始化为 0 保证 LoRA 初始时不改变模型行为（ΔW = BA = 0）

        self.lora_dropout = nn.Dropout(dropout)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        # 原始路径（不变）
        base_out = x @ self.weight.T

        # LoRA 路径（只训练这部分）
        lora_out = self.lora_dropout(x) @ self.lora_A @ self.lora_B * self.scale

        return base_out + lora_out

    @property
    def merged_weight(self) -> torch.Tensor:
        """推理时可以把 LoRA 合并进原始权重，消除推理开销"""
        return self.weight + (self.lora_A @ self.lora_B).T * self.scale


# 对比参数量
in_f, out_f, rank = 4096, 4096, 16

original = nn.Linear(in_f, out_f, bias=False)
lora     = LoRALinear(in_f, out_f, rank=rank)

trainable_original = sum(p.numel() for p in original.parameters())
trainable_lora     = sum(p.numel() for p in lora.parameters() if p.requires_grad)
total_lora         = sum(p.numel() for p in lora.parameters())

print(f"原始线性层参数量:        {trainable_original:,}")
print(f"LoRA 可训练参数量:        {trainable_lora:,}  ({trainable_lora/trainable_original:.1%})")
print(f"LoRA 总参数量（含冻结）:  {total_lora:,}")

# 验证：前向传播
x = torch.randn(2, 10, in_f)
out = lora(x)
print(f"\nLoRA 前向传播输出形状: {out.shape}")
```

### 3.2 QLoRA：在量化模型上应用 LoRA

QLoRA（Quantized LoRA）是 LoRA 的进一步改进：

1. 把基础模型量化到 4-bit（NF4 格式），大幅减少显存占用
2. 在量化模型上添加 BF16 精度的 LoRA 适配器
3. 只训练 LoRA 参数（4-bit 的基础模型仍然冻结）

效果：7B 模型的微调显存需求从 84 GB（全量 BF16）降至约 12 GB（QLoRA），普通消费级显卡（24 GB RTX 4090）可以完成微调。

QLoRA 通过将权重量化为 4-bit 精度来减少大型语言模型微调时的内存占用，同时不牺牲性能。LoRA 添加具有权重的适配器并冻结其余部分，而 QLoRA 将权重量化为 4-bit 精度来减少内存使用。

```python
# QLoRA 实战配置（需要 bitsandbytes 和 peft 库）
# pip install bitsandbytes peft transformers trl

from transformers import BitsAndBytesConfig

# 4-bit 量化配置
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,                        # 加载为 4-bit
    bnb_4bit_quant_type="nf4",               # NormalFloat4 量化（比 int4 效果好）
    bnb_4bit_compute_dtype=torch.bfloat16,   # 计算时临时反量化为 BF16
    bnb_4bit_use_double_quant=True,          # 双重量化（进一步节省显存）
)

# LoRA 配置
from peft import LoraConfig, get_peft_model, TaskType

lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=16,                        # LoRA 秩
    lora_alpha=32,               # 缩放系数
    target_modules=[             # 对哪些模块应用 LoRA
        "q_proj", "k_proj", "v_proj", "o_proj",   # Attention 投影
        "gate_proj", "up_proj", "down_proj",       # FFN
    ],
    lora_dropout=0.05,
    bias="none",
)

# 完整 QLoRA 加载流程
# from transformers import AutoModelForCausalLM, AutoTokenizer

# model = AutoModelForCausalLM.from_pretrained(
#     "meta-llama/Llama-3.2-3B",
#     quantization_config=bnb_config,   # 4-bit 量化
#     device_map="auto",
# )
# model = get_peft_model(model, lora_config)   # 添加 LoRA 适配器
# model.print_trainable_parameters()
# 输出类似：trainable params: 8,388,608 || all params: 3,221,225,472 || trainable%: 0.26%

print("QLoRA 显存对比（7B 模型）：")
configs = {
    "全量微调 BF16":      "~84 GB（需要 A100×2）",
    "全量微调 BF16+ZeRO": "~42 GB/GPU（需要 A100×2）",
    "LoRA BF16":          "~16 GB（A100×1）",
    "QLoRA 4bit":         "~12 GB（RTX 4090 可跑）",
}
for k, v in configs.items():
    print(f"  {k}: {v}")
```

### 3.3 全量微调 vs LoRA 的选择

LoRA 的学习能力显著低于全量微调，因为更新更少的参数限制了学习容量。学习新知识通常比将预训练基础模型转换为指令跟随模型需要更大的容量。

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 让模型学会新领域知识 | 全量微调 | 需要修改更多参数，LoRA 容量不够 |
| 改变回答格式/风格 | LoRA | 不需要学新知识，低秩更新就足够 |
| 资源受限（消费级 GPU）| QLoRA | 唯一可行方案 |
| 快速实验和迭代 | LoRA | 速度快，成本低 |
| 需要最佳性能 | 全量微调 | 效果上限更高 |

---

## 四、用 TRL 库进行 SFT 实战

### 4.1 完整 SFT 训练代码

SFTTrainer 让有监督微调开源 LLM 变得直接简便，它是 transformers 库 Trainer 的子类，支持所有相同特性，并额外提供：数据集格式化（包括对话和指令格式）、只对补全部分训练（忽略 prompt）、打包数据集以提高训练效率、PEFT 支持（包括 QLoRA）。

```python
from trl import SFTConfig, SFTTrainer
from peft import LoraConfig
from transformers import AutoModelForCausalLM, AutoTokenizer
from datasets import Dataset

# ── 准备数据 ──
def prepare_dataset() -> Dataset:
    """准备 SFT 训练数据（这里用少量示例）"""
    data = [
        {
            "messages": [
                {"role": "system",    "content": "你是一个专业的 AI 助手。"},
                {"role": "user",      "content": "什么是梯度下降？"},
                {"role": "assistant", "content": "梯度下降是优化算法，通过沿损失函数梯度的反方向更新参数来最小化损失。"},
            ]
        },
        {
            "messages": [
                {"role": "user",      "content": "解释一下 Transformer 的注意力机制"},
                {"role": "assistant", "content": "注意力机制让模型能动态地关注输入序列的不同部分..."},
            ]
        },
    ]
    return Dataset.from_list(data)

dataset = prepare_dataset()

# ── 模型和 Tokenizer ──
model_name = "Qwen/Qwen2.5-0.5B"   # 用小模型演示（真实场景用 7B+）
# model = AutoModelForCausalLM.from_pretrained(model_name, torch_dtype=torch.bfloat16)
# tokenizer = AutoTokenizer.from_pretrained(model_name)

# ── LoRA 配置 ──
peft_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "v_proj"],   # 只对 Q/V 做 LoRA（常用简化配置）
    lora_dropout=0.05,
    bias="none",
)

# ── 训练配置 ──
sft_config = SFTConfig(
    output_dir="./sft_output",
    num_train_epochs=3,
    per_device_train_batch_size=2,
    gradient_accumulation_steps=8,        # 等效 batch_size=16
    learning_rate=2e-4,                   # 比预训练低约 10 倍
    warmup_ratio=0.05,
    lr_scheduler_type="cosine",
    bf16=True,
    max_seq_length=2048,
    logging_steps=10,
    save_steps=100,
    save_total_limit=3,
    # SFT 关键配置
    dataset_kwargs={
        "skip_prepare_dataset": False,
    },
)

# ── 创建 SFTTrainer ──
# trainer = SFTTrainer(
#     model=model,
#     args=sft_config,
#     train_dataset=dataset,
#     peft_config=peft_config,    # 使用 LoRA
#     processing_class=tokenizer,
# )

# ── 开始训练 ──
# trainer.train()
# trainer.save_model()

print("SFT 训练配置摘要：")
print(f"  基础模型: {model_name}")
print(f"  LoRA rank: 16, alpha: 32")
print(f"  学习率: 2e-4（Cosine 调度 + 5% warmup）")
print(f"  数据格式: messages（ChatML）")
print(f"  Loss: 只对 assistant 回答部分计算")
```

---

## 五、SFT 的工程陷阱

### 5.1 常见错误与解决方案

```python
# 陷阱一：没有正确设置 Loss Masking
# 症状：模型开始"复读"指令而不是生成回答
# 原因：对指令部分也计算了 loss，模型学会了"生成指令"
# 解决：确保 DataCollator 设置了 response_template

# 陷阱二：Chat Template 不匹配
# 症状：SFT 之后模型回答乱七八糟，不按格式
# 原因：训练时用的 template 和推理时不一样
# 解决：用模型自带的 tokenizer.apply_chat_template

# 陷阱三：学习率过高
# 症状：SFT 之后模型在原有任务上性能下降（灾难性遗忘）
# 解决：使用 1e-5 到 2e-4 范围内的学习率，不要超过 5e-4

# 陷阱四：数据质量低
# 症状：模型学会了错误的知识或不恰当的表达方式
# 解决：宁可数据少但质量高，不要大量低质数据
# 经验：1000 条高质量数据 > 10000 条低质量数据

common_pitfalls = {
    "忘记 Loss Masking": "只计算 assistant 回答的 loss，指令部分设为 -100",
    "Chat Template 不一致": "训练和推理用同一个 tokenizer 的 apply_chat_template",
    "学习率过高": "SFT 学习率通常在 1e-5 到 2e-4，比预训练低 10-100 倍",
    "数据量不足但以为够了": "SFT 需要覆盖足够多样的指令类型，不只是数量",
    "忽略特殊 token": "确保 padding token 正确设置，避免 tokenizer 警告",
}

print("SFT 工程陷阱与解决方案：")
for k, v in common_pitfalls.items():
    print(f"  ⚠️  {k}")
    print(f"     解决：{v}\n")
```

### 5.2 SFT 数据质量 > 数量

SFT 中数据质量远比数量重要的原因：

```
通过高质量数据微调的流程（LIMA 论文，2023）：
  只用 1000 条精心挑选的指令数据
  效果接近用 52000 条 Alpaca 数据训练的模型

数据质量的关键维度：
  1. 多样性（Diversity）：覆盖不同任务类型、领域、难度
  2. 准确性（Accuracy）：回答是否正确，是否有幻觉
  3. 格式（Format）：是否符合目标使用场景的格式要求
  4. 清晰度（Clarity）：指令是否明确，回答是否清楚
  5. 边界安全（Safety）：是否包含不当内容
```

---

## 六、面试常见问题

**Q1：SFT 和预训练的本质区别是什么？**

预训练目标是让模型学习语言的统计规律和世界知识，在所有 token 上计算 loss，用万亿级 token 训练数周。SFT 目标是让模型学会按照指令格式回答，**只对回答部分（assistant 的输出）计算 loss**，指令部分设为 ignore，用数万到数百万条指令对训练数小时到数天，学习率比预训练低约 10-100 倍。

**Q2：为什么 SFT 只对回答部分计算 Loss？**

指令是给定的条件，不是需要预测的目标。如果对指令也计算 loss，模型会学习"如何生成指令"，而不是"如何回答指令"，导致训练目标错误。Loss 只计算在 assistant 的回答上，才能让模型学到"给定这个指令，应该如何响应"的映射关系。

**Q3：LoRA 的原理是什么？为什么它有效？**

LoRA 对每个目标权重矩阵 $W$（形状 $d \times d$），添加两个低秩矩阵 $A \in \mathbb{R}^{d \times r}$ 和 $B \in \mathbb{R}^{r \times d}$，前向传播时输出变为 $Wx + BAx \cdot \text{scale}$。$W$ 冻结不动，只训练 $A$ 和 $B$。有效的原因：大型语言模型的权重更新往往具有低内在秩，即权重更新矩阵可以由两个低秩矩阵近似，因此不需要修改全部参数也能实现有效的适配。

**Q4：LoRA 和全量微调各适合什么场景？**

LoRA 适合：改变模型的回答风格/格式、快速实验、资源受限（消费级 GPU）、需要多个任务的独立适配器（每个任务一套 LoRA 权重）。全量微调适合：需要让模型学习全新领域的大量知识、追求最佳效果（LoRA 学习能力受限于低秩约束）、有足够计算资源。

**Q5：QLoRA 是什么？和 LoRA 有什么区别？**

QLoRA 是 LoRA 的扩展：先把基础模型量化到 4-bit（NF4 格式），在量化模型上添加 BF16 精度的 LoRA 适配器，只训练 LoRA 参数。相比普通 LoRA（BF16 模型 + LoRA），QLoRA 把基础模型从 BF16（2字节/参数）压缩到 4-bit（0.5字节/参数），显存节省约 75%，让消费级显卡（24 GB）可以微调 7B 模型。性能损失极小。

**Q6：Chat Template 是什么？为什么不同模型要用不同的 Template？**

Chat Template 是把多轮对话结构化为模型输入文本的格式规范，包括角色标记（user/assistant/system）、特殊 token（如 `<|eot_id|>`）和轮次边界标记。不同模型在预训练和 SFT 时使用了不同的 Template，模型已经学会了识别特定格式中的角色信息。用错 Template 相当于把指令"藏"在模型识别不了的格式里，模型无法正确区分指令和回答的边界，导致输出混乱。

---

## 七、推荐学习资源

### 实战代码
- **HuggingFace TRL 文档** — https://huggingface.co/docs/trl/sft_trainer（SFTTrainer 的官方文档，含完整示例）
- **philschmid/deep-learning-pytorch-huggingface** — https://github.com/philschmid/deep-learning-pytorch-huggingface（大量实战 notebook，含 QLoRA 示例）
- **unsloth/unsloth** — https://github.com/unslothai/unsloth（QLoRA 的极速实现，比标准实现快 2× ，显存减少 70%）

### LoRA 相关论文
- "LoRA: Low-Rank Adaptation of Large Language Models" — Hu et al., 2022（LoRA 原论文）
- "QLoRA: Efficient Finetuning of Quantized LLMs" — Dettmers et al., 2023
- "LIMA: Less Is More for Alignment" — Zhou et al., 2023（证明 1000 条高质数据的有效性）
- "LoRA Learns Less and Forgets Less" — Biderman et al., 2024（LoRA vs 全量微调的系统对比）

### 数据集
- **Alpaca** — https://huggingface.co/datasets/tatsu-lab/alpaca
- **OpenHermes-2.5** — https://huggingface.co/datasets/teknium/OpenHermes-2.5
- **Magpie** — https://huggingface.co/datasets/Magpie-Align/Magpie-Pro-1M-v0.1

---

## 八、自测清单

- [ ] SFT 和预训练在 loss 计算上有什么区别？为什么这样设计？
- [ ] 什么是 Chat Template？为什么不同模型需要用不同的 Template？
- [ ] 手写 LoRA 的前向传播公式（$W' = W + BA$）
- [ ] LoRA 为什么把 B 初始化为全零？
- [ ] LoRA 的 rank=16，d_model=4096，可训练参数量是多少？（和全量微调相比减少了多少？）
- [ ] QLoRA 和 LoRA 的区别？QLoRA 能把 7B 模型的微调显存降到多少？
- [ ] LoRA 和全量微调各适合什么场景？
- [ ] SFT 数据质量和数量哪个更重要？举一个实际例子说明

---

**下一章预告**：5.2 奖励模型 — 从人类偏好数据出发，训练一个能给 LLM 回答打分的模型，是 RLHF 流程的关键中间件。
